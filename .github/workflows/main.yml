name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          java-version: '8'
          distribution: 'adopt'
        
      - name: Build with Ant
        run: ant -f build-auto.xml dist
        
      - name: Move builds to apporiate artifact folders
        run: cd dist;ls;rm -R -- */;cd ..;mkdir -p dist/{buildWin32,buildLinux,buildMacOS};mv dist/*windows* dist/buildWin32/;ls dist/;mv dist/*linux* dist/buildLinux/;ls dist/;mv dist/*macosx* dist/buildMacOS/;ls dist/;
        continue-on-error: true
      - uses: actions/upload-artifact@v2
        with:
          name: Win32 builds
          path: dist/buildWin32

      - uses: actions/upload-artifact@v2
        with:
          name: Linux builds
          path: dist/buildLinux

      - uses: actions/upload-artifact@v2
        with:
          name: Mac OS builds
          path: dist/buildMacOS